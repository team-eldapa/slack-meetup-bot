require('./datahelpers');
// const events = require('./seed-data');

// const request = require('supertest');
// const app = require('../lib/app');

describe('users routes tests', () => {
  it('is a passing test', () => {
    const something = 'hi';
    expect(something).toEqual('hi');
  });
});
